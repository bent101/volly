{
	"name": "volly",
	"version": "0.1.0",
	"workspaces": [
		"packages/*"
	],
	"scripts": {
		"pkill": "lsof -i :4852 | grep '(LISTEN)' | awk '{print $2}' | xargs kill -9",
		"sstdev": "sst dev",
		"dev": "npm run pkill && npm run sstdev"
	},
	"devDependencies": {
		"@tsconfig/node22": "^22",
		"typescript": "^5"
	},
	"dependencies": {
		"sst": "latest",
		"lodash": "^4.17.21"
	}
}
